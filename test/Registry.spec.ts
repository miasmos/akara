import { expect, assert } from 'chai';
import 'mocha';

describe('Registry', () => {
    describe('constructor()', () => {
        it('should take engine as a parameter', () => {
            
        })
    })
})